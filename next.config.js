/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/nowlistening-webpage',        // nom del repo de github per a GITHUB PAGES
  assetPrefix: '/nowlistening-webpage/'    // prefix per CSS/JS/assets
};

module.exports = nextConfig
